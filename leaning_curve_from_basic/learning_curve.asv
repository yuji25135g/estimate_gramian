datasets = ["dataset11_from_data", "dataset11_from_data"];
dataset_labels = [100, 200];
train_error = zeros(length(datasets));
test_error = zeros(length(datasets));
for i=1: length(datasets)
    [train_error(1,i), test_error(1,i)] = gramian_main(datasets(1,i));
end

figure()
plot(dataset_labels', train_error, 'o', dataset_labels, test_error, '+')